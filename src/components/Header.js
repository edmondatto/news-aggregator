import React from 'react';

const Header = () => (
  <nav className="navbar navbar-inverse bg-primary">
    <a className="navbar-brand" id="app-logo">Vortex News Aggregator</a>
  </nav>
);

export default Header;